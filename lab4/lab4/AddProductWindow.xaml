<Window x:Class="lab4.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab4"
        mc:Ignorable="d"
        Title="Додати Новий Товар" Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Артикул -->
        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">Артикул:</TextBlock>
        <TextBox Name="ArticleTextBox" Grid.Row="0" Grid.Column="1" Margin="0,5" />

        <!-- Назва -->
        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">Назва:</TextBlock>
        <TextBox Name="NameTextBox" Grid.Row="1" Grid.Column="1" Margin="0,5" />

        <!-- Одиниця виміру -->
        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">Одиниця виміру:</TextBlock>
        <TextBox Name="UnitTextBox" Grid.Row="2" Grid.Column="1" Margin="0,5" />

        <!-- Кількість -->
        <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">Кількість:</TextBlock>
        <TextBox Name="QuantityTextBox" Grid.Row="3" Grid.Column="1" Margin="0,5" />

        <!-- Ціна -->
        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">Ціна:</TextBlock>
        <TextBox Name="PriceTextBox" Grid.Row="4" Grid.Column="1" Margin="0,5" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Додати" Margin="0,0,10,0" Width="80" IsDefault="True" Click="AddButton_Click"/>
            <Button Content="Скасувати" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>